## 1. Validator module

- [x] 1.1 Implement the Pure-stdlib validator over a schema framework dependency providing source-document/v1 structural validation: a `validate_source_document(doc)` function checking every required top-level, page, and block field. Behavior: a conforming document returns ok; a document missing a field returns a structured error naming the field path (e.g. `pages[0].height`). Verify: unit tests for the valid-document and missing-field scenarios assert result and named path.
- [x] 1.2 Implement Facts-only enforcement via a reserved interpretation-key denylist satisfying the Facts-only constraint enforcement requirement: reject reserved interpretation keys (exhibit type, subtotal meaning, row role) inside a source document. Behavior: an interpretation key yields failure naming the disallowed key; a facts-only document passes. Verify: unit tests for the interpretation-key-rejected and observed-facts-allowed scenarios.

## 2. Fixtures

- [x] 2.1 Implement Fixtures generated by repo-owned code, not committed binaries where avoidable, satisfying the Deterministic repo-owned fixtures requirement: add deterministic fixtures covering pages, blocks, bbox, table cells, captions, headers, footers, and notes, plus one generated PDF fixture (title, section header, paragraph, table, caption, page header, page footer). Behavior: the test suite loads only repo-owned fixtures and no real EMBA PDF. Verify: the fixtures-drive-validation-tests scenario — a grep/assertion confirms no test references a real case PDF path.
- [x] 2.2 Wire the generated PDF fixture through ingestion and validation. Behavior: the ingested fixture exercises pages, a table with cells, a caption, a page header, and a page footer and passes structural validation. Verify: unit test asserts those elements present and `validate_source_document` returns ok. **Note:** the committed fixture `tests/fixtures/source_document_v1_real.json` is the source_document produced by running the generated PDF through the real Docling adapter once; tests validate that committed artifact deterministically rather than invoking Docling in CI.

## 3. Optional CLI and regression

- [x] 3.1 Add a thin CLI wrapper that validates a `source_document.json` file path and exits non-zero on failure with the structured errors. Behavior: validating a malformed or non-conforming file exits non-zero with a parse/field error; a conforming file exits zero. Verify: subprocess test asserts exit codes and error text for a good and a bad fixture.
- [x] 3.2 Run the full suite to confirm no regression. Behavior: existing adapter and generator tests stay green alongside the new validation tests. Verify: `python3 -m unittest discover -s tests` exits OK.
